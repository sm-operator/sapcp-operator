os: linux

language: go

go:
  - "1.14.x"
install:
  - ./hack/install-kubebuilder.sh
  - go get golang.org/x/tools/cmd/cover
  - go get github.com/mattn/goveralls

jobs:
  include:
    - name: "Tests"
      script:
        - make test